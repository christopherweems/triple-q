import XCTest
@testable import triple_q

final class triple_qTests: XCTestCase {
    static var allTests = [
        ("testExample", testExample),
    ]
}

extension triple_qTests {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertTrue(!false)
    }
    
}
